syntax = "proto2";
import "common.proto";
package wayside;

// Feedback message from matcher to pcd and video sources.
message MatcherFeedback {
    optional uint64 accepted_max_timestamp = 1;
    optional uint64 commit_timestamp = 4;
    optional bool inclusive = 2;
    repeated DevicePath accepted_devices = 3;
}
