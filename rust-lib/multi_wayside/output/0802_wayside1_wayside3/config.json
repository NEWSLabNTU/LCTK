{
    "using_same_face_of_marker": false, 
    "output_dir": "output",
    "pcap1_config" :{
	"sensor": "vlp32",
	"file_path" : "/home/vince/NTU/Wayside/recording_0802_lidar_AC/wayside_1/pcd/2021-08-02T13:44:00.721486736+08:00/lidar1.pcap",
	"frame_selected" : null,// Setting this field to null will let program randomly select frames to detect board. You can use ESC key to skip the frame and use Return key to choose the frame.
	"detection_config" : {
		"board_detector": {
			// max number of RANSAC steps
			"plane_ransac_max_iterations": 500,
			// the loss threshold that a point is considered an inlier
			"plane_ransac_inlier_threshold": 0.05,
			// max number of ICP iterations
			"max_icp_iterations": 20000,
			// pose weight is amount of pose change per iteration
			// the ICP terminates if pose weight is blow this threshold several times
			"icp_pose_weight_threshold": 5e-13,
			// the maximum accepted ICP loss
			"icp_rejection_threshold": 1.0,
			// the length of border margin
			"board_width_mm": 1000,    // mm
			// the radius of circle holes
			"hole_radius_mm": 150,     // mm
			// suppose the center of board is (0, 0)
			// the center of hole will be at (+/- shift, +/- shift)
			"hole_center_shift_mm": 200, // mm
		    },
		"filters": [
		    {
			// this filter rejects all points outside of this bbox
			"inclusive_range_filter": {
			    "x_min": 11000,
			    "x_max": 15000,
			    "y_min": 4500,
			    "y_max": 7000,
			    "z_min": -1200,
			    "z_max": 800
			}
		    },
		    {
			// this filter rejects all points inside of this bbox
			"exclusive_range_filter": {
			    "x_min": -100,
			    "x_max": 100,
			    "y_min": -100,
			    "y_max": 100,
			    "z_min": -100,
			    "z_max": 100
			}
		    },
		    {
			// this filter rejects all points below this intensity
			"intensity_filter": {
			    "min_intensity": 0
			}
		    }
		],
	    }
	},

    "pcap2_config":{
	"sensor": "vlp32",
	"file_path": "/home/vince/NTU/Wayside/recording_0802_lidar_AC/wayside_3/pcd/2021-08-02T13:44:00.779125656+08:00/lidar1.pcap",
	"frame_selected" : null,
	"detection_config" : {
		"board_detector": {
			// max number of RANSAC steps
			"plane_ransac_max_iterations": 500,
			// the loss threshold that a point is considered an inlier
			"plane_ransac_inlier_threshold": 0.05,
			// max number of ICP iterations
			"max_icp_iterations": 20000,
			// pose weight is amount of pose change per iteration
			// the ICP terminates if pose weight is blow this threshold several times
			"icp_pose_weight_threshold": 5e-13,
			// the maximum accepted ICP loss
			"icp_rejection_threshold": 1.0,
			// the length of border margin
			"board_width_mm": 1000,    // mm
			// the radius of circle holes
			"hole_radius_mm": 150,     // mm
			// suppose the center of board is (0, 0)
			// the center of hole will be at (+/- shift, +/- shift)
			"hole_center_shift_mm": 200, // mm
		    },
		"filters": [
		    {
			// this filter rejects all points outside of this bbox
			"inclusive_range_filter": {
			    "x_min": 12000,
			    "x_max": 15000,
			    "y_min": 2500,
			    "y_max": 6500,
			    "z_min": -1200,
			    "z_max": 800
			}
		    },
		    {
			// this filter rejects all points inside of this bbox
			"exclusive_range_filter": {
			    "x_min": -100,
			    "x_max": 100,
			    "y_min": -100,
			    "y_max": 100,
			    "z_min": -100,
			    "z_max": 100
			}
		    },
		    {
			// this filter rejects all points below this intensity
			"intensity_filter": {
			    "min_intensity": 0
			}
		    }
		],
	    }
	}
}
