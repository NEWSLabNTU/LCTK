# Detection Archive fixtures

`solved_v4.json` and `solved_v5.json` intentionally contain byte-for-byte equal
`transform` objects.  V4 is export-only legacy data; v5 binds the same transform to
the solid target's Target Identity for restoration.
